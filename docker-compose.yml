services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sahayak
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - sahayak-postgres:/var/lib/postgresql/data
volumes:
  sahayak-postgres:
