{
  "installCommand": "npm --prefix apps/web install --include=dev --package-lock=false",
  "buildCommand": "npm --prefix apps/web run build",
  "outputDirectory": "apps/web/dist",
  "framework": "vite"
}
